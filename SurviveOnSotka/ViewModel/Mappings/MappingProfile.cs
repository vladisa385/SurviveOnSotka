using AutoMapper;

namespace SurviveOnSotka.ViewModel.Mappings
{
    public class MappingProfile : Profile
    {
        public MappingProfile()
        {
            //// Add as many of these lines as you need to map your objects
            //CreateMap<TypeFoodViewModel, TypeFood>().ForMember(
            //    property => property.Name,
            //    property => property.MapFrom(oldproperty => oldproperty.Name));


            //CreateMap<TypeFood, TypeFoodViewModel>().ForMember(
            //    "Name",
            //    prp => prp.MapFrom(oldproperty => oldproperty.Name));

        }
    }
}
